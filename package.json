{
  "name": "quiz-api",
  "version": "1.0.0",
  "description": "An open-source quiz API that returns a JSON of quiz questions, four options for each question along with the correct one. \n To start the REST API server, simply execute \"npm start\"",
  "main": "index.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bluebird": "^3.7.2",
    "express": "^4.17.1",
    "mysql": "^2.18.1",
    "node-fetch": "^2.6.1"
  }
}
